export interface RegistrationModel {
  firstName?: string;
  lastName?: string;
  email?: string;
  password?: string;
  confirmPassword?: string;
  newsletter?: boolean;
  acceptedTerms?: boolean;
}
